# FLAPS | Fibonacci Lean Agile Pointing System

This build uses the provided Southwest-Airlines-Emblem.png as the app icon (only resized for different icon sizes).

## Run

```bash
npm install
npm start
```
Test

Open http://localhost:3000
